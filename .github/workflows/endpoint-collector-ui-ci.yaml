name: endpoint-collector-ui service ci

on:
  push:
    branches: [ "main" ]
    paths:
      - ".github/workflows/actions/action.yaml"
      - ".github/workflows/endpoint-collector-ui-ci.yaml"
      - ".github/workflows/reusable-ui-service-ci.yaml"
      - "services/external-endpoint-collector/endpoint-collector-ui/**"
      - "parent-pom/**"
      - "base-platform/**"
  pull_request:
    branches: [ "main" ]
    paths:
      - ".github/workflows/actions/action.yaml"
      - ".github/workflows/endpoint-collector-ui-ci.yaml"
      - ".github/workflows/reusable-ui-service-ci.yaml"
      - "services/external-endpoint-collector/endpoint-collector-ui/**"
      - "parent-pom/**"
      - "base-platform/**"
      
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

jobs:
  ci:
    uses: ./.github/workflows/reusable-ui-service-ci.yaml
    with:
      service-name: "endpoint-collector-ui"
      service-path: "services/external-endpoint-collector/endpoint-collector-ui"
      docker-image-name: "endpoint-collector-ui"
      docker-context: "./services/external-endpoint-collector/endpoint-collector-ui"
      dockerfile-path: "./services/external-endpoint-collector/endpoint-collector-ui/Dockerfile"
    secrets:
      ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
